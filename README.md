## Interface

Qt Widgets-приложение «Панель управления».

### Сборка

В корне проекта:

```bash
qmake
make
```

Собранные файлы попадают в папку `build/`.

### Запуск

После сборки:

```bash
./build/interface
```

